# [Project name] — CLAUDE.md

> Copy this file to `<your-project>/.claude/CLAUDE.md` (or the project root as
> `CLAUDE.md`) and fill in the brackets. Delete any section you don't need —
> a short file that's true beats a long file that's aspirational.

## What this project is

[One paragraph. What the product does, who it's for, and the current stage
(prototype / pre-launch / live with users). The agent reads this first — make it
load-bearing, not marketing.]

## Ground truth

- **Run the app:** `[command]`
- **Run the tests:** `[command]` — [N] tests, keep them green
- **Stack:** [languages / frameworks / DB — one line]
- **Entry points:** [the 2–3 files where work usually starts]

## Non-negotiables

- [e.g. "Never touch the production database file at ~/app/data.db"]
- [e.g. "All statistics results must carry a re-runnable script beside them"]
- [e.g. "No new dependencies without asking me first"]

## Do NOT

- [e.g. "Do not start work in the frozen/ or legacy/ directories"]
- [e.g. "Do not 'clean up' code unrelated to the current task"]
- [e.g. "Do not commit — I review and commit manually"]

## Definition of done

A change is done when: [e.g. "tests pass, the real app was run and the feature
was reached from the UI, and the plan file (if any) was updated"]. A summary
saying it works is not the same as it working — verify end-to-end.
